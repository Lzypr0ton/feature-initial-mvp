# Development

Install Node.js 22+, then run `npm install`, `npm run playwright:install`, and `npm run dev`. Create a rule by implementing `AuditRule` and registering it in `packages/rules/src/index.ts`; add a source reference and tests. Never mark a GIGW rule automated without a deterministic method and an authoritative reference.
