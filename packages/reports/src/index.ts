import type { ScanResult } from '@gigw/shared';
const escape=(s:string)=>s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]!));
export function htmlReport(scan:ScanResult){const rows=(scan.ruleResults||[]).map(r=>`<tr><td>${escape(r.ruleId)}</td><td><b>${r.status}</b></td><td>${escape(r.severity)}</td><td>${escape(r.message)}</td><td>${escape(r.recommendation)}</td></tr>`).join('');return `<!doctype html><html lang="en"><meta charset="utf-8"><title>GIGW assessment</title><style>body{font:14px system-ui;margin:2rem;color:#172033}table{border-collapse:collapse;width:100%}td,th{border:1px solid #ccd;padding:.6rem;text-align:left}th{background:#eef3ff}.notice{background:#fff5d6;padding:1rem;border-left:4px solid #c58900}</style><body><h1>GIGW Compliance Checker</h1><p class="notice">Automated assessment — not an official GIGW/STQC certification.</p><p><b>Target:</b> ${escape(scan.url)}<br><b>Scan:</b> ${escape(scan.id)}</p><h2>Automated assessment</h2><pre>${escape(JSON.stringify(scan.summary,null,2))}</pre><table><thead><tr><th>Rule</th><th>Status</th><th>Severity</th><th>Finding</th><th>Recommendation</th></tr></thead><tbody>${rows}</tbody></table><h2>Limitations</h2><ul>${scan.limitations.map(x=>`<li>${escape(x)}</li>`).join('')}</ul></body></html>`;}
